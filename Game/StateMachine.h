#include <stdio.h>
#include <iostream>
#include <SFML/Graphics.hpp>
#include "Char.h"

using namespace CharState;

class StateMachine {
    void processState(STATE currentState ) { 
        switch(state) {
            
        }
    }
};